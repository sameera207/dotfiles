name: rails-tmux
project_root: .
windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim
        - bundle exec rails s
        - bundle exec rails c
  - logs: tail -f log/development.log
